MNIST MLP MODEL TRAINER

---

This code implements a simple MLP model to classify the MNIST digit dataset.
It was trained on a DigitalOcean server using only CPUs. The included mailbot was built
to notify the user after the training ended.

To train a new model, configure your wanted parameters in grid_search_trainer.py and call
>> python3 grid_search_trainer.py

---

January 2020
Christian Mollière